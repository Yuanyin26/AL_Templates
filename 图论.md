[TOC]

# 图论

## $Dijkstra$(单源最短路径)

```c++
void dijkstra(int st)
{
    int n;
    vector<int>dis(n + 1,INF);
    vector<bool>vis(n + 1);
	dis[st] = 0;
	priority_queue<PA>q;
	q.push({-dis[st],st});
	while(!q.empty())
	{
		auto [d,u] = q.top();q.pop();
		if(vis[u])continue;
		vis[u] = 1;
		for(int i = heads[u];i;i = e[i].nx)
		{
			int v = e[i].to;   
            if(vis[v])continue;
			if(dis[v] > dis[u] + e[i].w)
			{
				dis[v] = dis[u] + e[i].w;
				q.push({dis[v],v});
			}
		}
	}
}
```

## $SPFA$(用于负环判断，差分约束)

```c++
bool SPFA()
{
	while(!q.empty())
    {
        int u = q.front();
        q.pop();
        vis[u] = 0;
        for(int i = heads[u];i;i = e[i].nx)
        {
            int v = e[i].to;
            if(dis[u] + e[i].w < dis[v])
            {
                dis[v] = dis[u] + e[i].w;
                if(!vis[v])
                {
                    q.push(v);
                    vis[v] = 1;
                    //计算最短路时可不需要下面
                    cnt[v]++;
                    if(cnt[v]>n)return true;
                }	
            }
        }	
    }
	return false;
}
//差分约束建边
求解一个变量序列最大值或最小值，可通过建立对应差分约束系统
求解最短路径或者最长路径来实现,其最短路或最长路就是这个变量可能的最大值或最小值
最短路径i + k >= j,add(i,j,k);
最长路径i + k <= j,add(i,j,k);
```

## Prim(最小生成树)

```c++
void prim()
{
	int tot = 0,ans = 0;
	priority_queue<PA>q;
    vector<int>dis(n + 1,INF);
    vector<bool>vis(n + 1);
    dis[1] = 0;
    q.push({-dis[1],1});
	while(!q.empty())
	{
		auto [d,u] = q.top();q.pop();
        d = -d;
		if(d != dis[u])continue;
		vis[u] = 1;
		ans += dis[u];
		tot++;
		for(int i = heads[u];i;i = e[i].nx)
		{
			int v = e[i].to;
			if(!vis[v] && dis[v] > e[i].w)
			{
				dis[v] = e[i].w;
				q.push({v,dis[v]});
			}
		}
	}
	if(tot != n)ans = -1;//无法生成一颗树
}
```

## $Kurskal$

```c++
void kruskal()
{
    sort(edge,edge + m,cmp);
    for(re int i = 0;i < m;i++)
    {
        eu = find(edge[i].u), ev = find(edge[i].v);
        if(eu == ev)
        {
            continue;
        }
        ans += edge[i].w;
        fa[ev] = eu;
        if(++cnt == n - 1)
        {
            break;
        }
    }
}
```

## Kruskal重构树

```c++
std::vector<std::array<int,3>>edge(m);
std::sort(edge.begin(),edge.end());
std::vector g(2 * n ,std::vector<int>());
std::vector<int>W(2 * n);
dsu ds = dsu(2 * n);//并查集
int tot = n;
for(auto [w,u,v] : edge)
{
	int pu = ds.find(u);
	int pv = ds.find(v);
	if(pu == pv)continue;
	ds.fa[pu] = ++tot;
	ds.fa[pv] = tot;
	W[tot] = w;
	g[tot].push_back(pu);
	g[tot].push_back(pv);
}
```



## 缩点SCC

```c++
int low[N],dfn[N],times = 0;
int belong[N],tot,dis[N];//缩点后每个点的归属，缩点后数量,缩点的权值
bool vis[N];//当前点是否正在被缩
stack<int>st;
void tarjan(int p)
{
	low[p] = dfn[p] = ++times;
	vis[p] = 1;
	st.push(p);
	for (int i = heads[p];i; i = e[i].nx)
	{
		int v = e[i].to;
		if (!dfn[v])
		{
			tarjan(v);
			low[p] = min(low[p], low[v]);
		}
		else if (vis[v])
			low[p] = min(low[p], dfn[v]);
	}
	if (low[p] == dfn[p])
	{
		tot++;
		while (1)
		{
			int x = st.top();
			st.pop();
			belong[x] = tot;
			vis[x] = 0;
			if (x == p)break;
		}
	}
}
```

## 边双连通分量E-DCC

```c++
struct edge
{
    int nx;
    int to;
}e[M];int heads[N],cntqq = 1;
void add(int u,int v)
{
    e[++cntqq] = {heads[u],v};
    heads[u] = cntqq;
}
void solve()
{   
    //初始化
    cntqq = 1;
    memset(heads,0,sizeof(int) * (n + 1));
    
    std::vector<int>low(n + 1),dfn(n + 1),bel(n + 1);//属于哪个缩点
    std::vector<bool>bz(2 * (m + 1));//桥
    std::vector<std::vector<int>>e_dcc(n + 1);//缩点的集合
    int tot = 0,times = 0;
    auto tarjan = [&](auto &&self,int u,int p) -> void
    {
        low[u] = dfn[u] = ++times;
        for (int i = heads[u];i;i = e[i].nx)
        {
            int v = e[i].to;
            if (!dfn[v])
            {
                self(self,v,i);
                if(low[v] > dfn[u])bz[i] = bz[i ^ 1] = 1;
                low[u] = std::min(low[u], low[v]);
            }
            else if(i != (p ^ 1))
                low[u] = std::min(low[u], dfn[v]);
        }
    };
    auto dfs1 = [&](auto &&self,int u,int id) -> void
    {
        bel[u] = id;
        e_dcc[id].push_back(u);
        for(int i = heads[u];i;i = e[i].nx)
        {
            int v = e[i].to;
            if(bel[v] || bz[i])continue;
            self(self,v,id);
        }
    };
    for(int i = 1;i <= n;i++)
        if(!dfn[i])tarjan(tarjan,i,0);
    for(int i = 1;i <= n;i++)
        if(!bel[i])dfs1(dfs1,i,++tot);
}
```

## 点双连通分量V-DCC

```c++
std::vector<int>v_dcc[N];
int tot,times;
void solve()
{
    tot = times = 0;
    int n,m;
    std::cin >> n >> m;
    std::vector<std::vector<int>>g(n + 1);
    std::vector<int>low(n + 1),dfn(n + 1),cut(n + 1);
    for(int i = 1;i <= m;i++)
    {
        int u,v;
        std::cin >> u >> v;
        if(u == v)continue;
        g[u].push_back(v);
        g[v].push_back(u);
    }
    std::stack<int>stk;
    auto tarjan = [&](auto &&self,int u,int rt) -> void
    {
        low[u] = dfn[u] = ++times;
        stk.push(u);
        if(g[u].empty())//孤立点
        {
            v_dcc[++tot].push_back(u);
            return;
        }
        int cnt = 0;//子树数量
        for(int v : g[u])
        {
            if(!dfn[v])
            {
                self(self,v,rt);
                low[u] = std::min(low[u],low[v]);
                if(low[v] >= dfn[u])//作为割点
                {
                    cnt++,tot++;
                    while(1)
                    {
                        int x = stk.top();stk.pop();
                        v_dcc[tot].push_back(x);
                        if(x == v)break;
                    }
                    v_dcc[tot].push_back(u);
                }
            }
            else low[u] = std::min(low[u],dfn[v]);
        }
        cut[u] = cnt > (u == rt);
    };
    for(int i = 1;i <= n;i++)
        if(!dfn[i])tarjan(tarjan,i,i);
    std::cout << tot << std::endl;
    for(int i = 1;i <= tot;i++)
    {
        int len = v_dcc[i].size();
        std::cout << len << " ";
        for(int j = 0;j < len;j++)
            std::cout << v_dcc[i][j] << " \n"[j == len - 1];
    }
}
```

## 割点

```c++
std::vector<int>cut(n + 1),low(n + 1),dfn(n + 1);
int times = 0;
auto tarjan = [&](auto &&self,int u,int rt) -> void
{
    dfn[u] = low[u] = ++times;
    int cnt = 0;//子树数量
    for(int i = heads[u];i;i = e[i].nx)
    {
        int v = e[i].to;
        if(!dfn[v])
        {
            self(self,v,rt);
            low[u] = std::min(low[u],low[v]);
            if(low[v] >= dfn[u])cnt++;
        }
        else
            low[u] = std::min(low[u],dfn[v]);
    }
    cut[u] = cnt > (rt == u);
};
```

## 割边

```c++
//初始化
cntqq = 1;

std::vector<bool>bz(2 * (m + 1));//从编号 2 开始
std::vector<int>low(n + 1),dfn(n + 1);
int times = 0;
//p代表到达u的边编号
auto tarjan = [&](auto &&self,int u,int p) -> void
{
    low[u] = dfn[u] = ++times;
    for (int i = heads[u];i;i = e[i].nx)
    {
        int v = e[i].to;
        if (!dfn[v])
        {
            self(self,v,i);
            if(low[v] > dfn[u])bz[i] = bz[i ^ 1] = 1;
            low[u] = std::min(low[u], low[v]);
        }
        else if(i != (p ^ 1))//不走反向边
            low[u] = std::min(low[u], dfn[v]);
    }
};
```

## 二分图判断(判奇环)

```c++
vector<bool>vis(n + 1);
vector<int>col(n + 1);
auto dfs = [&](auto &&self,int u) -> bool
{
	vis[u] = true;
	for(int i = heads[u];i;i = e[i].nx)
	{
        int v = e[i].to;
        if(vis[v])
        {
       		if(col[u] == col[v])return false;
        }
        else
        {
			col[v] = col[u] ^ 1;
			if(!self(self,v))return false;
		}
	}
	return true;
};
auto check = [&]() -> bool
{  
    for(int i = 1;i <= n;i++)
        if(!vis[i])
        {
            col[i] = 0;
            if(!dfs(dfs,i))return false;//是奇环(不是二分图)
        }
    return true;
}
```



## 最大二分图匹配

```c++
//匈牙利算法
bool vis[N][N],ask[N];
int matched[N];
int n, m, e,cnt = 0;
inline bool found(int x)
{
	for (int i = 1; i <= m; i++)
	{
		if (vis[x][i])
		{
			if (ask[i])continue;
			ask[i] = 1;
			if (!matched[i] || found(matched[i]))
			{
				matched[i] = x;
				return true;
			}
		}
	}
	return false;
}

void match()
{
	for (int i = 1; i <= n; i++)
	{
		memset(ask, 0, sizeof ask);
		if (found(i))
			cnt++;
	}
}
void solve()
{
	cin >> n >> m >> e;
	for (int i = 1; i <= e; i++)
	{
		int u, v;
		cin >> u >> v;
		vis[u][v] = 1;
	}
	match();
	cout << cnt << endl;
}
signed main() {
	IOS;
	int T = 1; //cin >> T;
	while (T--) {
		solve();
	}
}
//dinic算法(单位容量下，nsqrt的复杂度，优于匈牙利)
源点指向左部连边
右部指向汇点连边
左部指向右部连边
跑出来的最大流也就是最大匹配
```



## 最大流

```c++
struct MF
{
    struct edge
    {
        int nx;
        int to;
        int cap;
        int flow;
    }e[M << 1];
    int heads[N],cur[N],cntqq = 0,dep[N];
    int mxflow = 0;
    int n,S,T;
    void init(){memset(heads,-1,sizeof(int) * (n + 1));}
    void add(int u,int v,int w)
    {
        e[cntqq] = {heads[u],v,w,0};
        heads[u] = cntqq++;
        e[cntqq] = {heads[v],u,0,0};
        heads[v] = cntqq++;
        return ;
    }
    MF()
    {
        int m;
        cin >> n >> m >> S >> T;
        init();
        for(int i = 1;i <= m;i++)
        {
            int u,v,w;
            cin >> u >> v >> w;
            add(u,v,w);
        }
    }

    bool bfs()
    {
        queue<int>q;
        memset(dep,0,sizeof(int) * (n + 1));
        dep[S] = 1;
        q.push(S);
        while(!q.empty())
        {
            int u = q.front();q.pop();
            for(int i = heads[u];~i;i = e[i].nx)
            {
                int v = e[i].to;
                if((!dep[v]) && (e[i].cap > e[i].flow))
                {
                    dep[v] = dep[u] + 1;
                    q.push(v);
                }
            }
        }
        return dep[T];
    }
    int dfs(int u,int flow)
    {
        if(u == T||(!flow))return flow;
        int res = 0;
        for(int& i = cur[u];~i;i = e[i].nx)
        {
            int v = e[i].to,d;
            if((dep[v] == dep[u] + 1) && (d = dfs(v,min(flow - res,e[i].cap - e[i].flow))))
            {
                res += d;
                e[i].flow += d;
                e[i^1].flow -= d;
                if(res == flow)return res;
            }
        }
        return res;
    }
    void dinic()
    {
        while(bfs())
        {
            memcpy(cur,heads,sizeof(int) * (n + 1));
            mxflow += dfs(S,inf);
        }
    }
};
```

## 最小费用最大流

```c++
struct edge
{
    int nx;
    int to;
    int cap;
    int flow;
    int cost;
}e[M << 1];int heads[N],cntqq = 0;
void add(int u,int v,int w,int c)
{
    e[cntqq] = {heads[u],v,w,0,c};
    heads[u] = cntqq++;
    e[cntqq] = {heads[v],u,0,0,-c};
    heads[v] = cntqq++;
    return;
}
struct MF
{
    int n,m,S,T;
    int dis[N],cur[N],vis[N];
    int mxflow = 0,mnco = 0;
    MF()
    {
        std::cin >> n >> m >> S >> T;
        memset(vis,0,sizeof(int) * (n + 1));
        memset(heads,-1,sizeof(int) * (n + 1));
        for(int i = 1;i <= m;i++)
        {
            int u,v,w,c;
            std::cin >> u >> v >> w >> c;
            add(u,v,w,c);
        }
    }
    bool spfa()
    {
        memset(dis,inf,sizeof(int) * (n + 1));
        dis[S] = 0;vis[S] = 1;
        std::queue<int>q;
        q.push(S);
        while(!q.empty())
        {
            int u = q.front();q.pop();
            vis[u] = 0;
            for(int i = heads[u];~i;i = e[i].nx)
            {
                int v = e[i].to;
                if(e[i].cap > e[i].flow && dis[v] > dis[u] + e[i].cost)
                {
                    dis[v] = dis[u] + e[i].cost;
                    if(!vis[v]){q.push(v);vis[v] = 1;}
                }
            }
        }
        return dis[T] != INF;
    }
    int dfs(int u,int flow)
    {
        if(u == T||(!flow))return flow;
        int res = 0;
        vis[u] = 1;
        for(int& i = cur[u];~i;i = e[i].nx)
        {
            int v = e[i].to,d;
            if(!vis[v] && dis[v] == dis[u] + e[i].cost && (d = dfs(v,std::min(flow - res,e[i].cap - e[i].flow))))
            {
                res += d;
                e[i].flow += d;
                e[i^1].flow -= d;
                mnco += d * e[i].cost;
                if(res == flow)
                {
                    vis[u] = 0;
                    return res;
                }
            }
        }
        vis[u] = 0;
        return res;
    }
    void dinic()
    {
        while(spfa())
        {
            memcpy(cur,heads,sizeof(int) * (n + 1));
            mxflow += dfs(S,INF);
        }
    }
};
void solve()
{
    MF x = MF();
    x.dinic();
    cout << x.mxflow << " " << x.mnco << endl;
}
```

## 欧拉回路/路径

```c++
//记得判连通
//两种都是对于字典序最小的欧拉回路/路径的输出
//对于无向图
int n,m;
cin >> n >> m;
vector<vector<PA>>g(n + 1);
vector<int>du(n + 1),heads(n + 1);
vector<bool>vis(m + 1);
for(int i = 1;i <= m;i++)
{
    int u,v;
    cin >> u >> v;
    g[u].push_back({v,i});
    g[v].push_back({u,i});
    du[u]++;
    du[v]++;
}
int st = 0,ed = 0;
for(int i = 1;i <= n;i++)
{
    if(du[i] & 1)
    {
        if(!st)st = i;
        else if(!ed)ed = i;
        else
        {
            cout << "No" << endl;
            return;
        }
    }
}
st = st ? st : 1;
for(int i = 1;i <= n;i++)
    sort(g[i].begin(),g[i].end());
stack<int>stk;
auto dfs = [&](auto &&self,int x) -> void
{
    for(int &i = heads[x];i < g[x].size();)
    {
        auto [v,op] = g[x][i++];
        if(vis[op])continue;
        vis[op] = 1;
        self(self,v);
    }
    stk.push(x);
};
dfs(dfs,st);
while(!stk.empty())
{
    cout << stk.top() << " ";
    stk.pop();
}
cout << endl;
//对于有向图
int n,m;
cin >> n >> m;
vector<vector<int>>g(n + 1);
vector du(n + 1,vector<int>(2));
vector<int>heads(n + 1);
for(int i = 1;i <= m;i++)
{
    int u,v;
    cin >> u >> v;
    g[u].push_back(v);
    du[v][0]++;
    du[u][1]++;
}
int st = 0,ed = 0;
bool op = 0;
for(int i = 1;i <= n;i++)
{
    if(du[i][0] == du[i][1])continue;
    else if(du[i][1] - du[i][0] == 1)
    {
        if(!st)st = i;
        else op = 1;
    }
    else if(du[i][0] - du[i][1] == 1)
    {
        if(!ed)ed = i;
        else op = 1;
    }
    else op = 1;
}
if(op)
{
    cout << "No" << endl;
    return;
}
for(int i = 1;i <= n;i++)
    sort(g[i].begin(),g[i].end());
st = st ? st : 1;
stack<int>stk;
auto dfs = [&](auto && self,int x) -> void
{
    for(int &i = heads[x];i < g[x].size();)
    {
        int v = g[x][i++];
        self(self,v);
    }
    stk.push(x);
};
dfs(dfs,st);
while(!stk.empty())
{
    cout << stk.top() << " ";
    stk.pop();
}
cout << endl;
```

