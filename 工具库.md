## 离散化

```c++
template<class T>
struct discrete
{
    std::vector<T>a;
    int len;
    discrete(std::vector<T> arr,int st = 0)
    {
        std::sort(arr.begin() + st,arr.end());
        arr.erase(std::unique(arr.begin() + st,arr.end()),arr.end());
        arr.assign(arr.begin() + st,arr.end());
        a = std::move(arr);
        len = a.size();
    }
    //第几小
    int operator[](T x){
        int pos = std::lower_bound(a.begin(),a.end(),x) - a.begin();
        return pos + 1;
    }
};
```

